# Supabase (client)
VITE_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-publishable-key

# Base URL for client-side absolute links and auth redirects
VITE_PUBLIC_BASE_URL=http://localhost:3000

# Optional: Override auth redirect URL (useful for switching environments)
# Examples:
# Development: VITE_AUTH_REDIRECT_OVERRIDE=http://localhost:3000
# Preview/Staging: VITE_AUTH_REDIRECT_OVERRIDE=https://your-preview-url.lovable.app
# Production: Leave unset to use VITE_PUBLIC_BASE_URL or window.location.origin
# VITE_AUTH_REDIRECT_OVERRIDE=

# Ads Configuration
VITE_ADS_ENABLED=true

# Google AdSense Configuration
VITE_ADSENSE_EDGE_LEFT_SLOT=your-edge-left-ad-slot-id
VITE_ADSENSE_EDGE_RIGHT_SLOT=your-edge-right-ad-slot-id
VITE_ADSENSE_MOBILE_FEED_SLOT=your-mobile-feed-ad-slot-id

# Supabase (server)
# Used by API routes for privileged operations
SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
SUPABASE_ANON_KEY=your-anon-publishable-key # optional; used for debug API to simulate RLS

# Base URL for server-side link generation
PUBLIC_BASE_URL=http://localhost:3000

# Development flag to allow draw with unauthenticated participants (server-side only)
# Set to '1' to bypass authentication validation in draw API (development only)
# DEV_ALLOW_UNAUTH_DRAW=1

# Amazon Affiliate Configuration
VITE_AMAZON_PARTNER_TAG=yourtag-21

# Server-side Amazon PA-API Configuration (for production)
AMAZON_API_ENABLED=false
AMZ_ACCESS_KEY=your-access-key-id
AMZ_SECRET_KEY=your-secret-access-key
AMZ_ASSOC_TAG=yourtag-21
AMZ_REGION=eu-west-1
